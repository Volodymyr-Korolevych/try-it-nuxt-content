import tryItBlock from './tryItBlock.vue'
import tryItButton from './tryItButton.vue'
import tryItPage from './tryItPage.vue'
import tryItImg from './tryItImg.vue'

export default {
  tryItBlock,
  tryItButton,
  tryItPage,
  tryItImg
}
